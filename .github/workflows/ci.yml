name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: -D warnings

jobs:
  fmt:
    name: rustfmt
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - run: cargo fmt --all -- --check

  clippy:
    name: clippy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
      # Clippy runs for visibility but doesn't fail CI on style-
      # level lints (needless_range_loop, while_let_loop,
      # type_complexity, etc). Genuine bugs are still caught by
      # the rustc warnings gate above (RUSTFLAGS: -D warnings)
      # and by the test suite.
      - run: cargo clippy --all-targets --all-features

  test:
    name: test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo build --all-targets
      - run: cargo test --all-targets

  audit:
    name: cargo-audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Install cargo-audit
        run: cargo install --locked cargo-audit
      # RUSTSEC-2023-0071 — Marvin Attack timing side-channel in `rsa`
      # crate. Reaches us transitively via the arti-client / Tor crates
      # (wire_onion.rs). No upstream fix is available yet (RustSec
      # entry says "No fixed upgrade is available"). DRIFT doesn't
      # expose RSA decryption to attacker-controlled timing
      # measurements directly — the Tor stack uses it internally for
      # onion-service crypto. Re-evaluate when arti or rsa publishes
      # a patched version.
      - run: cargo audit --ignore RUSTSEC-2023-0071

  docs:
    name: rustdoc
    runs-on: ubuntu-latest
    env:
      RUSTDOCFLAGS: -D warnings
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo doc --no-deps --all-features
